class Solution:
    # 1 refers to the leftmost or topmost point
    def intersect(self, a1, a2, b1, b2):
        pass
    def computeArea(self, A: int, B: int, C: int, D: int, E: int, F: int, G: int, H: int) -> int:
        points1 = [(A,B), (A,D), (C,D), (C,B)]
        points2 = [(E,F), (E,H), (G,H), (G,F)]